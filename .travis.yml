language: cpp
os:
  osx

jobs:
  include:
    - os: osx
      osx_image: xcode11
      dist: trusty
      addons:
        homebrew:
          packages:
            - ninja
            - cmake
            - openssl

script:
  - cmake -DCMAKE_EXPORT_COMPILE_COMMANDS:BOOL=TRUE -DCMAKE_BUILD_TYPE:STRING=Debug -H. -B./build -G "Ninja"
  - cmake --build ./build
